Temporary CI-only branch for verifying the 0.6.6 launcher-icon WIP. The canonical source remains on Google Drive. The image files generated by the workflow are compile-only placeholders and must never be used in a release APK.